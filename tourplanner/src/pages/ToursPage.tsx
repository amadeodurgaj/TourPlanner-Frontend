import { Navbar } from "@/components/Navbar";
import { useState } from "react";


export default function ToursPage() {
    
    return (
        <div>
<h1 className="sm:mx-auto sm:w-full sm:max-w-sm flex items-center justify-center font-mono text-3xl font-bold tracking-tight text-[var(--secondary)] content-center justify-center">Tours    Page</h1>
        </div>
    );
}